import { SearchByFieldPipe } from './search-by-field.pipe';

describe('SearchByFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
